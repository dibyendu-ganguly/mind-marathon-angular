import { ScrollToFirstInvalidFormFieldDirectiveDirective } from './scroll-to-first-invalid-form-field-directive.directive';

describe('ScrollToFirstInvalidFormFieldDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToFirstInvalidFormFieldDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
