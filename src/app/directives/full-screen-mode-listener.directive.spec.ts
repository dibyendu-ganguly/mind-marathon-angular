import { FullScreenModeListenerDirective } from './full-screen-mode-listener.directive';

describe('FullScreenModeListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new FullScreenModeListenerDirective();
    expect(directive).toBeTruthy();
  });
});
